# Hanshal Goyal

Personal portfolio hosted at https://hanshalg.github.io/.

Static HTML, CSS, and JavaScript. Edit `app.js` for portfolio content, `style.css` for styling, and `index.html` for the masthead and navigation. Original raster graphics are in `assets/`.

GitHub Pages publishes the root of the `main` branch. No build step is required.
